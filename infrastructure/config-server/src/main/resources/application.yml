spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/GodSpeedsT/banking-configurations.git
          default-label: main
          search-paths: '*'

server:
  port: 8888